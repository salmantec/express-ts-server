{
  "name": "ts-server",
  "version": "1.0.0",
  "description": "Simple expressJS + typescript server",
  "main": "server.js",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "start": "npm run build && node dist/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohamed Salman",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.2",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "typescript": "^3.3.3333"
  }
}
